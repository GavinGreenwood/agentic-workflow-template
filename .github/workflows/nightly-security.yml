name: Nightly Security

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  security:
    uses: ./.github/workflows/security.yml
    permissions:
      contents: read
      security-events: write
    secrets: inherit
