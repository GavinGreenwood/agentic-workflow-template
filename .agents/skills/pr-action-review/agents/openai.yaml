interface:
  display_name: "PR Action Review"
  short_description: "Action every review on one pull request"
  default_prompt: "Use $pr-action-review with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
