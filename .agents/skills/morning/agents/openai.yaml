interface:
  display_name: "Morning"
  short_description: "Run daily repository maintenance"
  default_prompt: "Use $morning with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
