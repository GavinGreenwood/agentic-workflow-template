interface:
  display_name: "My PR Action Loop"
  short_description: "Action reviews across all of your open PRs"
  default_prompt: "Use $pr-action-review-mine-loop with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
