interface:
  display_name: "Multi Repo"
  short_description: "Manage isolated parallel repository slots"
  default_prompt: "Use $multi-repo with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
