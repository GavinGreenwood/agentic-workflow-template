interface:
  display_name: "Ship PR"
  short_description: "Verify, push, open, and review a pull request"
  default_prompt: "Use $pr with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
