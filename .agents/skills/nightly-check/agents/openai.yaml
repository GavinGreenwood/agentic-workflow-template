interface:
  display_name: "Nightly Check"
  short_description: "Inspect and action scheduled CI failures"
  default_prompt: "Use $nightly-check with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
