interface:
  display_name: "Push"
  short_description: "Verify, commit, and push the current branch"
  default_prompt: "Use $push with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
