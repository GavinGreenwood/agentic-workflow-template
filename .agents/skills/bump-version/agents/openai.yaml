interface:
  display_name: "Bump Version"
  short_description: "Bump and push main's minor version tag"
  default_prompt: "Use $bump-version with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
