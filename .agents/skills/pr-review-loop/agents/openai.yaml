interface:
  display_name: "PR Review Loop"
  short_description: "Review teammates' open pull requests"
  default_prompt: "Use $pr-review-loop with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
