interface:
  display_name: "QA Review Action"
  short_description: "Classify and action Jira QA feedback"
  default_prompt: "Use $qa-review-action with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
