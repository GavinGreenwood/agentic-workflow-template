interface:
  display_name: "Wrap Up"
  short_description: "Return safely to an up-to-date main branch"
  default_prompt: "Use $wrap-up with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
