interface:
  display_name: "Main"
  short_description: "Return safely to an up-to-date main branch"
  default_prompt: "Use $main with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
