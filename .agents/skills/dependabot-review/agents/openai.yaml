interface:
  display_name: "Dependabot Review"
  short_description: "Review and action Dependabot pull requests"
  default_prompt: "Use $dependabot-review with the arguments and repository context I provide."
policy:
  allow_implicit_invocation: false
